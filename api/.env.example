# Chuỗi kết nối PostgreSQL (tự host, RDS, hoặc Supabase: Project Settings → Database)
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/tramcan_etax?schema=public"

# CORS — origin của Vite dev (có thể nhiều, cách nhau dấu phẩy)
WEB_ORIGIN=http://localhost:5173

# JWT (thêm khi implement auth)
# JWT_SECRET=
# JWT_EXPIRES_IN=7d
